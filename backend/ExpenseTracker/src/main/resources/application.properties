spring.application.name=ExpenseTracker

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/expense_tracker_db
spring.datasource.username=root
spring.datasource.password=zack

# JDBC Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate Configuration
#Disable the below line when in production
spring.jpa.hibernate.ddl-auto=update

#Enable the below line if the project is in production
#spring.jpa.hibernate.ddl-auto=none

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

#base path for storing contents 
file.storage.base-path=D:/ExpenseTracker/storage/
